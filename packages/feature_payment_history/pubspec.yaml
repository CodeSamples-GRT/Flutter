name: feature_payment_history
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1

dependency_overrides:
  intl: ^0.19.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  app_common:
    path: ../app_common
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  freezed_annotation: ^2.0.3
  localization:
    path: ../localization
  network:
    path: ../network

dev_dependencies:
  build_runner: ^2.1.7
  dependency_validator: ^3.2.2
  freezed: ^2.0.3+1
  mockito: ^5.0.17


